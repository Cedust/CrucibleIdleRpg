interface:
  display_name: Handoff
  short_description: Prepare a concise handoff for another agent
  default_prompt: Use $handoff to prepare a handoff for the next session.
policy:
  allow_implicit_invocation: false
