/**
 * Platzhalter-Kampfbildschirm. Die eigentliche Kampf-Engine (reine, deterministische,
 * seedbare Logik) und das Runden-Playback folgen — Simulation und Rendering bleiben
 * strikt getrennt (siehe AGENTS.md §5).
 */
export function CombatScreen() {
  return (
    <section>
      <h2 className="text-xl font-semibold">Combat</h2>
      <p className="mt-2 text-text-muted">The arena awaits. Combat engine coming soon.</p>
    </section>
  );
}
